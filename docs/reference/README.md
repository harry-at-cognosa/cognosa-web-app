# docs/reference — environment snapshots

Package freezes and requirement lists captured from the working checkout
(`~/cognosa_web_app`) on the dates in the filenames. Kept for parity debugging
only; `requirements.txt` at the repo root is authoritative.

- `pip_freeze_venv_251227.txt` — `venv/` (python.org 3.12.8) as of 2025-12-27.
- `pip_freeze_halvenv_251227.txt` — a second venv (`halvenv`) from the same date.
- `requirements_251124_1228.txt` — earlier requirements snapshot, 2025-11-24.
- `cwa_top_level_requirements.txt` — hand-maintained list of top-level deps.
